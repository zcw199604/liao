# 原生 Android 客户端任务清单

> 方案类型：功能开发
> 选定方案：Jetpack Compose 模块化架构
> 状态说明：`[ ]` 待执行 / `[√]` 已完成 / `[X]` 执行失败 / `[-]` 已跳过 / `[?]` 待确认

---

## 任务依赖说明
- `0 -> 1 -> 2 -> 3 -> 4 -> 5 -> 6 -> 7`
- `6 -> 8 / 9 / 10`
- `2 / 5 / 6 / 7 / 8 / 9 / 10 -> 11 -> 12 -> 13`

说明：
- 任务 2（网络与鉴权）是全部业务模块的前置依赖
- 任务 5（WebSocket 与聊天主链路）依赖任务 3（身份）与任务 4（会话列表）
- 任务 8/9/10 依赖媒体主链路与基础网络能力完成后再推进

---

## 0. 方案准备
- [√] 确认 Android 工程创建位置、包名、签名方式与最低版本要求
- [√] 确认服务端联调地址策略（局域网 / 生产域名 / 可切换环境）
- [√] 确认网页端“功能一致”的验收口径（能力一致 / UI 风格近似）
- [√] 确认 Android 端默认复刻 `reportReferrer`、`ShowUserLoginInfo`、`warningreport` 等网页端主动动作

## 1. 工程初始化
- [√] 创建 Android 原生工程骨架（Gradle、app、基础模块、依赖管理）
- [√] 集成 Compose、Navigation、Hilt、Retrofit、OkHttp、Room、DataStore、Coil、WorkManager
- [√] 建立通用 Result、异常、日志、调试开关与基础主题

## 2. 网络与鉴权基础设施
- [√] 实现 BaseUrl 配置、Authorization 拦截器、401 处理与通用网络错误映射
- [√] 按后端能力拆分 API Service（auth / identity / chat / favorite / media / mtphoto / douyin / system / videoExtract）
- [√] 建立 token 持久化、冷启动校验与登录态恢复能力
- [√] 建立下载代理、文件流读取、`resolveImagePort` 与大文件传输能力

## 3. 身份模块
- [√] 实现身份列表、创建、快速创建、删除、编辑、选择
- [√] 实现最近身份恢复与登录后跳转链路
- [√] 为 WebSocket `sign` 登录消息准备 identity -> sign payload 映射

## 4. 会话列表模块
- [√] 实现历史列表、收藏列表、用户信息展示与进入聊天页
- [√] 实现列表刷新、空态、错误态与全局收藏入口
- [√] 明确历史列表、收藏列表与未读计数的本地缓存策略

## 5. WebSocket 与聊天主链路
### 5.1 协议对齐
- [√] 建立 WebSocket code/act 协议目录，覆盖 `-4 / -3 / 7 / 12 / 13 / 14 / 15 / 16 / 18 / 19 / 30`
- [√] 对齐消息方向判断规则：`fromuser.id === md5(identity.id)`
- [√] 对齐 `sign`、`ShowUserLoginInfo`、`warningreport`、输入状态相关主动动作

### 5.2 连接与状态机
- [√] 实现 `/ws?token=` 连接、`sign` 身份绑定、断线重连与 forceout 处理
- [√] 对齐 forceout 为 **5 分钟** 禁止重连，而非 80 秒
- [√] 建立连接状态流：Idle / Connecting / Connected / Reconnecting / Forceout / Closed

### 5.3 消息仓库与历史归并
- [-] 实现聊天页消息流、历史分页、消息去重与本地发送状态管理
  > 备注: 本次执行先落地 HTTP 历史加载、本地消息缓存与最小文本发送；完整分页、去重与乐观状态机留待下一轮补齐。
- [-] 统一归并 WebSocket 回显、HTTP 历史、乐观发送消息
  > 备注: 当前只建立了 Room 缓存与 WS 原始消息接入点，尚未完成三路消息归并策略。
- [-] 处理输入状态、匹配成功、在线状态查询结果等非文本消息
  > 备注: `ShowUserLoginInfo` / `warningreport` 已封装发送入口，但非文本消息解析与 UI 呈现暂未展开。

### 5.4 聊天交互
- [-] 实现文本消息、表情、收藏切换、清空本地记录、拉黑操作
  > 备注: 本次仅完成最小文本发送与页面输入区，表情、收藏、清空记录、拉黑操作未在首轮骨架中展开。
- [√] 实现 `reportReferrer` 初始化上报与聊天页会话初始化流程
- [-] 实现聊天头部、输入区、上传菜单、历史媒体入口与必要弹层
  > 备注: 已落地聊天头部与输入区，上传菜单、历史媒体入口和弹层留待媒体主链路完成后补充。

## 6. 媒体主链路
- [-] 实现图片/视频选择、上传、上传状态反馈与失败重试
  > 备注: 当前只建立 `MediaApiService` 与工程目录预留，未进入上传 UI 与重试闭环。
- [-] 实现所有上传媒体页、聊天历史媒体页、删除/批量删除/重传
  > 备注: 依赖媒体主链路 UI 与本地媒体缓存结构，本轮未展开。
- [-] 实现图片查重、视频封面/预览、媒体发送流程
  > 备注: 已预留接口拆分，但未实现页面与业务编排。
- [-] 处理设备可达地址、图片服务地址与 `resolveImagePort` 兼容逻辑
  > 备注: 已在网络层预留 `resolveImagePort` Service，真正的设备可达地址策略仍需结合上传与预览联调实现。

## 7. 设置与辅助功能
- [-] 实现设置页/抽屉：身份信息、图片服务配置、系统配置读取与更新
  > 备注: 当前设置页已支持 Base URL、Token、当前身份快照，系统配置与图片服务专属设置尚未接入。
- [-] 实现连接统计、断开全部连接、forceout 清理等系统管理能力
  > 备注: 已拆分 `SystemApiService`，但管理操作 UI 与调用流程未在首轮骨架中落地。
- [-] 实现全局收藏页与常用辅助入口
  > 备注: 收藏与辅助入口将在会话 / 媒体功能稳定后追加。

## 8. mtPhoto 模块
- [-] 实现相册/目录浏览、缩略图加载、面包屑/收藏夹能力
  > 备注: 依赖媒体主链路与独立页面设计，本轮仅预留 `MtPhotoApiService`。
- [-] 实现 mtPhoto 媒体导入到上传链路
  > 备注: 媒体导入依赖上传闭环，当前条件未满足。
- [-] 验证 `/api/getMtPhotoThumb`、原图下载与目录收藏行为
  > 备注: 当前仓库环境缺少 Android 工具链与真机/模拟器联调条件，暂未执行。

## 9. 抖音模块
- [-] 实现抖音解析、作品预览、下载代理与导入上传
  > 备注: 本轮只完成 `DouyinApiService` 与目录预留，未进入页面实现。
- [-] 实现收藏作者/作品/标签管理与同步能力
  > 备注: 依赖二期扩展页面与本地缓存结构，当前未展开。
- [-] 实现外部能力不可用时的降级提示
  > 备注: 需要结合真实服务可用性探测实现，当前只在设计与文档中明确。
- [-] 验证下载代理、封面代理与导入上传的兼容性
  > 备注: 当前仓库环境缺少 Android 构建链与外部服务联调条件，暂未执行。

## 10. 视频抽帧模块
- [-] 实现抽帧输入选择（本地上传 / mtPhoto）与任务创建
  > 备注: 本轮仅拆分 `VideoExtractApiService`，未进入 UI 与任务管理实现。
- [-] 实现任务列表、详情分页、取消、继续、删除
  > 备注: 依赖媒体与任务中心页面，本轮未展开。
- [-] 验证长任务轮询、失败恢复与文件清理策略
  > 备注: 需要 Android 端实际运行环境，本次未执行。

## 11. 本地缓存与恢复
- [-] 落地 Room 缓存结构（身份、会话、消息、收藏、媒体、任务、系统配置）
  > 备注: 当前只落地身份 / 会话 / 消息三类基础表，媒体、任务与系统配置缓存待后续扩展。
- [-] 完成页面恢复、离线展示、前后台切换状态保持
  > 备注: 已具备 DataStore + Room 基础，但尚未做完整恢复策略与离线展示细化。
- [-] 明确缓存失效与清理策略
  > 备注: 本轮只完成结构预留，缓存淘汰策略仍需结合真实使用情况补充。

## 12. 质量与验证
- [-] 补充核心单元测试（网络映射、消息去重、WebSocket 状态机、MD5 self-detection）
  > 备注: 已新增 `ProtocolAlignmentTest` 覆盖 MD5 self-detection、forceout 常量、已知 code/act 解析与私信 envelope 分类；完整网络映射 / 状态机测试待后续补充。
- [-] 补充关键 UI / 集成测试（登录、身份、聊天、上传、forceout）
  > 备注: 当前仓库环境未安装 Android SDK / JDK 17，无法执行 Compose UI 与集成测试。
- [-] 与 Go 服务完成联调验证并整理问题清单
  > 备注: 已运行 `go test ./...` 验证现有 Go 代码未受影响，但 Android 客户端尚未完成真机 / 模拟器联调。
- [-] 对抖音、mtPhoto、抽帧做能力可用性验证与降级演练
  > 备注: 依赖二期扩展模块与真实运行环境，本轮未执行。

## 13. 文档与交付
- [√] 输出 Android 模块说明、运行方式、联调配置与已知限制
- [√] 输出 WebSocket 协议摘要与关键接口兼容说明
- [√] 完成首轮验收并回写知识库 / CHANGELOG

---

## 执行总结
- 首轮执行聚焦“工程骨架 + 主流程页面 + 协议基线”，已在 `android-app/` 建立 Compose/Hilt/Retrofit/Room/DataStore/WebSocket 基础设施。
- 媒体、mtPhoto、抖音、视频抽帧、本地缓存深化与 Android 侧自动化验证因工具链与后续业务范围条件未满足，统一标记为“已跳过”，建议后续单独创建二期方案包继续推进。
- 已同步更新 `.helloagents/modules/android-client.md`、`project.md`、`CHANGELOG.md`、`modules/api.md`、`modules/arch.md`、`modules/overview.md` 等知识库文档。
- 2026-03-13 第二轮验收修复已补齐身份编辑/删除、会话列表显式空态/错误态/全局收藏入口，以及 WS 最小协议目录、`code=-4/-3` forceout、真实自动重连与聊天页 Info 查询入口；详见 `../202603130550_android-client-acceptance-fixes/`。
- 已执行验证：`go test ./...` 通过；Android 侧因当前环境缺少 JDK / Android SDK，未执行 Gradle 编译与设备联调。
- 2026-03-13 第二轮验收修复已由 `202603130601_android-identity-chatlist-fix` 与 `202603130550_android-ws-protocol-reconnect` 落地；后续 Claude 只读复核结论为“有条件通过”，且无 P0/P1 剩余问题。
