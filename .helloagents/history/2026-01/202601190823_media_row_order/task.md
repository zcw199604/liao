# 轻量迭代任务清单：media_row_order

目标：修复媒体列表在“按时间排序”场景下的视觉顺序，使其按“每一行从左到右”展示，避免瀑布流列优先导致顺序误解。

## 任务

- [√] 调整 `InfiniteMediaGrid` 的 masonry 布局为行优先（按行从左到右渲染）。
- [√] 更新知识库（Media/mtPhoto 模块文档）说明布局顺序策略。
- [√] 更新 `helloagents/CHANGELOG.md`（Unreleased -> 修复）。

## 验证

- [?] 手动验证：打开“全站图片库/mtPhoto 相册”，在瀑布流模式下确认从左到右按时间顺序展示。
  > 备注: 需要在实际页面中人工确认（本次未在容器内启动前端进行验证）。

